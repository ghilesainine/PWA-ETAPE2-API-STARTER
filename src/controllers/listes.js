export async function list(req,res){ res.status(501).json({error:'Lister listes (optionnel)'})}
export async function detail(req,res){ res.status(501).json({error:'Consulter par code_partage'})}
export async function create(req,res){ res.status(501).json({error:'Créer liste (générer code)'} )}
export async function update(req,res){ res.status(501).json({error:'À implémenter'})}
export async function remove(req,res){ res.status(501).json({error:'À implémenter'})}
